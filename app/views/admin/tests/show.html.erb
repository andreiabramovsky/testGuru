<h2><%= @test.title %></h2><hr>
  <div>
    <p><%= t('.level') %> <%= @test.level %></span>
    <p><%= t('.category') %> <%= @test.category.title %></span>
  </div>

  <table>
    <th><%= t('.questions') %></th>
    <th colspan="3"><%= t('.actions') %></th>
    <%= render @questions %>
  </table>

<%= link_to t('.back'), admin_tests_path %></span>
<%= link_to t('.new_question'), new_admin_test_question_path(@test) %>
